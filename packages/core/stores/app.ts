// import { create } from "zustand";
// import { ComponentData, Path } from "../types";
// import { AppContext } from "../components/Puck/context";

// export type DataIndex = AppContext;

// export const usePuckStore = create<{
//   index: DataIndex;
//   setIndex: (newIndex: DataIndex) => void;
// }>((set) => ({
//   index: {},
//   setIndex: (newIndex) => set((state) => ({ ...state, index: newIndex })),
// }));
